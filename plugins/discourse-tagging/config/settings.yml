plugins:
  tagging_enabled:
    default: true
  max_tags_per_topic:
    default: 5
    client: true
  max_tag_length:
    default: 20
    client: true
  min_trust_to_create_tag:
    default: 0
    enum: 'TrustLevelSetting'
